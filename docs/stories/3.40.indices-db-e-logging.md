# Story 3.40 — Índices de Performance e Logging de Abusividade

## Status: Ready for Review

## Contexto

Análise de performance identificou duas melhorias críticas:
1. Queries de `vendas_calculos` sem índices compostos — full table scan em tabelas com milhões de linhas
2. O serviço `abusividade_relatorio_service.py` não registra timing, volume processado nem falhas parciais, dificultando diagnóstico de lentidão

---

## Story

**Como** administrador do sistema,
**Quero** que queries de vendas respondam rápido e que o processamento de abusividade seja rastreável,
**Para** garantir SLA aceitável em relatórios e facilitar diagnóstico de problemas de performance.

---

## Acceptance Criteria

### AC1 — Índices compostos em `vendas_calculos`
- [ ] Índice composto criado: `(calc_id, data_venda)`
- [ ] Índice composto criado: `(bandeira, forma_pagamento)`
- [ ] Índice simples criado: `(cliente_id)` (se ainda não existir)
- [ ] Migração Alembic criada e aplicada
- [ ] `EXPLAIN` nas queries de abusividade e comparativo confirma uso dos índices

### AC2 — Logging no serviço de abusividade
- [ ] Log no início do processamento: `"Iniciando cálculo abusividade cliente={id} período={inicio}..{fim}"`
- [ ] Log no fim: `"Concluído cliente={id} linhas={n} tempo={elapsed:.2f}s"`
- [ ] Log para cada lote processado (se houver paginação): `"Lote {n} processado: {count} registros"`
- [ ] Exceções capturadas com `logger.exception(...)` — stack trace completo no log
- [ ] Nível de log: `INFO` para início/fim, `DEBUG` para lotes, `ERROR` para exceções

### AC3 — Índice em `relatorio_tasks`
- [ ] Índice composto criado: `(cliente_id, status, created_at DESC)`
- [ ] Melhora query de listagem de relatórios pendentes

### AC4 — Testes
- [ ] Teste confirma que migração aplica e reverte sem erro (pós-deploy com DB real)
- [x] Teste de unidade para logging: verifica que `logger.info` é chamado com os campos esperados

---

## Tarefas

### Backend
- [x] Criar migração Alembic: `add_indexes_vendas_calculos_relatorio_tasks`
  - `ix_vendas_calculos_calc_data`: `(calc_id, data_venda)`
  - `ix_vendas_calculos_bandeira_forma`: `(bandeira, forma_pagamento)`
  - `ix_relatorio_tasks_status_created`: `(status, created_at)`
- [x] Editar `abusividade_relatorio_service.py`:
  - Adicionar `import logging` + `logger = logging.getLogger(__name__)`
  - Adicionar log de início, fim e exceções nos pontos de entrada do processamento
- [ ] Validar com `EXPLAIN` que índices são utilizados (pós-deploy)

---

## Impacto / Dependências

- Depende de: Story 3.32 (histórico abusividade) ✅, Story 3.36 (dashboard KPIs) ✅
- Impacta: performance de relatórios de abusividade e dashboard
- Risco: baixo — apenas índices e logs, sem alteração de lógica

---

## File List

- `apps/api/alembic/versions/0001_add_indexes_vendas_calculos.py` (novo)
- `apps/api/app/services/abusividade_relatorio_service.py` (modificar)
- `apps/api/tests/unit/test_abusividade_logging.py` (novo)
