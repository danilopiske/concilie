# Story 3.22 — Dashboard com Filtro de Período

**Status:** Done
**Branch:** feature/nextjs-migration
**Sprint:** 3

## Objetivo

Adicionar filtro de período (7, 30, 90 dias) ao dashboard executivo, atualizando os KPIs dinamicamente. Inclui endpoint de atividade semanal e mini gráfico de barras CSS-only.

## Acceptance Criteria

- [x] Parâmetro `periodo` (default=30) no endpoint `GET /dashboard/resumo`
- [x] KPI "Processamentos" filtra pelo período selecionado
- [x] Novo endpoint `GET /dashboard/atividade-semanal` — contagem de importações por semana (últimas 4)
- [x] Seletor de período (7/30/90 dias) no header do dashboard
- [x] Mudança de período dispara re-fetch dos dados automaticamente
- [x] Componente `MiniBarChart` CSS-only exibe tendência semanal
- [x] ruff check: 0 erros
- [x] Nenhum endpoint existente quebrado (default=30 preserva comportamento anterior)

## Tasks

- [x] Backend: adicionar parâmetro `periodo` ao `/dashboard/resumo`
- [x] Backend: novo endpoint `/dashboard/atividade-semanal`
- [x] Frontend: atualizar `dashboardApi.getResumo(periodo)` e adicionar `getAtividadeSemanal()`
- [x] Frontend: criar componente `MiniBarChart`
- [x] Frontend: integrar seletor de período no header do dashboard
- [x] Frontend: integrar `MiniBarChart` na página
- [x] QA: ruff check 0 erros

## File List

### Modified
- `apps/api/app/api/v1/endpoints/dashboard.py` — parâmetro `periodo`, timedelta, endpoint `atividade-semanal`
- `apps/web/src/lib/api/dashboard.ts` — `getResumo(periodo)`, tipos e `getAtividadeSemanal()`
- `apps/web/src/app/(dashboard)/dashboard/page.tsx` — seletor de período, estado, re-fetch, MiniBarChart

### Created
- `apps/web/src/components/dashboard/MiniBarChart.tsx` — componente de mini gráfico de barras CSS-only
- `docs/stories/3.22.dashboard-periodo.md` — esta story
