# Story 3.19 — Página de Status do Sistema

**Status:** Done
**Branch:** feature/nextjs-migration

## Objetivo

Página `/status` (autenticada) mostrando saúde do sistema em tempo real com auto-refresh a cada 30 segundos.

## Acceptance Criteria

- [x] Endpoint `/api/v1/sistema/status` retorna conectividade da API e do banco
- [x] Contagens totais: clientes, importações, cálculos, relatórios
- [x] Contagem de tarefas ativas (PROCESSING ou PENDING)
- [x] Últimas 3 tarefas de cada tipo (importações, cálculos, relatórios)
- [x] Página `/status` no frontend com cards visuais de status
- [x] Auto-refresh a cada 30 segundos + botão de atualização manual
- [x] Link "Status do Sistema" adicionado na sidebar (seção "Sistema")

## QA Results

- ruff check: 0 erros
- `Activity` icon já importado no Sidebar — reutilizado sem duplicação
- Dashboard existente não tem endpoint `/status` — sem duplicação

## File List

### Backend
- `apps/api/app/api/v1/endpoints/sistema.py` — novo endpoint `/sistema/status`
- `apps/api/app/api/v1/api.py` — registro do router `sistema`

### Frontend
- `apps/web/src/lib/api/sistema.ts` — API client com interface `SistemaStatus`
- `apps/web/src/app/(dashboard)/status/page.tsx` — página de status
- `apps/web/src/components/layout/Sidebar.tsx` — seção "Sistema" adicionada
