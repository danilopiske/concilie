# Story 3.36 — Dashboard: Painel de KPIs Executivos

## Status: Done

## Story

**Como** usuário do sistema,
**Quero** ver um painel de KPIs executivos no dashboard principal,
**Para** ter uma visão consolidada e rápida dos números mais importantes do sistema.

## Acceptance Criteria

- [x] `GET /dashboard/kpis` retorna JSON com 8 KPIs consolidados
- [x] KPIs usam campos reais dos modelos (verificados antes da implementação)
- [x] Frontend exibe painel com 6 cards de KPI no topo do dashboard
- [x] Cards possuem ícone, cor e label descritivo
- [x] Dados são carregados assincronamente com estado de loading
- [x] 0 erros ruff antes do commit

## Tasks

- [x] Ler modelos: vendas.py, contestacao.py, extrato_cliente.py, abusividade_task.py, import_task.py
- [x] Ler dashboard.py (endpoint) e dashboard.ts (client)
- [x] Verificar localização da página dashboard no frontend
- [x] Implementar endpoint `GET /dashboard/kpis` no backend
- [x] Adicionar schema `DashboardKpis` em `schemas/dashboard.py`
- [x] Adicionar `getKpis()` em `lib/api/dashboard.ts`
- [x] Criar `components/dashboard/KpiPanel.tsx`
- [x] Integrar `<KpiPanel />` na página do dashboard
- [x] Ruff 0 erros
- [x] Commit

## Campos reais dos modelos (verificados)

| Campo Story | Modelo | Campo Real |
|---|---|---|
| valor_total_vendas_mes | Venda.valor_venda | `valor_venda` |
| created_at vendas | Venda.data_processamento | `data_processamento` |
| contestacao status | Contestacao.status | default='rascunho' |
| extrato status | ExtratoCliente.status | 'aguardando' | 'importado' | 'divergente' |
| abusividade critico | AbusividadeTask.status | 'error' (não há 'critico') |
| import created_at | ImportTask.created_at | `created_at` |
| taxa_recuperacao | VendasCalculos.tx_venda | média de `tx_venda` |

## File List

- `apps/api/app/schemas/dashboard.py` — adicionado DashboardKpis
- `apps/api/app/api/v1/endpoints/dashboard.py` — adicionado GET /dashboard/kpis
- `apps/web/src/lib/api/dashboard.ts` — adicionado getKpis()
- `apps/web/src/components/dashboard/KpiPanel.tsx` — novo componente
- `apps/web/src/app/(dashboard)/dashboard/page.tsx` — integrado KpiPanel
- `docs/stories/3.36.dashboard-kpis-executivos.md` — esta story
