# Story 3.42 — Índices FK Faltantes + print() → logger

## Status: Done

## Contexto

Dois quick wins identificados na revisão do sistema:
1. **3 FKs sem índice** que causarão full table scan em produção
2. **12 `print()`** em services críticos que bypassam o sistema de logging centralizado

Agrupados numa story pequena por serem independentes e de baixo risco.

---

## Story

**Como** desenvolvedor,
**Quero** que FKs críticas tenham índices e que logs sejam estruturados,
**Para** garantir performance em produção e observabilidade dos serviços.

---

## Acceptance Criteria

### AC1 — Índices FK
- [x] `import_task.cliente_id` — adicionar `index=True` no modelo + índice na migração 0003
- [x] `usuario_clientes.cliente_id` — adicionar `index=True` no modelo + índice na migração 0003
- [x] `usuario_contextos.contexto_id` — adicionar `index=True` no modelo + índice na migração 0003
- [x] Migração 0003 com `upgrade()` e `downgrade()` corretos

### AC2 — print() → logger
- [x] `import_service.py` — print() redundante removido (l.123); log_cb → logger.info (l.172)
- [x] `ai_service.py` — substituído por `logger.warning/error`
- [x] `relatorio_service.py` — substituído por `logger.info/error`
- [x] `abusividade_service.py` — substituído por `logger.warning`
- [x] Cada arquivo deve ter `logger = logging.getLogger(__name__)` no topo

### AC3 — Testes
- [x] Suite de 62 testes continua passando após as mudanças

---

## Tarefas

### Backend
- [x] Editar modelos: `import_task.py`, `usuario_cliente.py`, `usuario_contexto.py` — adicionar `index=True`
- [x] Criar `alembic/versions/0003_add_fk_indexes.py`
- [x] Editar `import_service.py` — print() removido (linha 123) e log_cb → logger.info (linha 172)
- [x] `ai_service.py`, `relatorio_service.py`, `abusividade_service.py` — print → logger concluído

---

## Impacto / Dependências

- Sem dependências
- Risco: baixo — substituição mecânica
- Esforço estimado: 1-2 horas

---

## File List

- `apps/api/app/models/import_task.py` (modificar)
- `apps/api/app/models/usuario_cliente.py` (modificar)
- `apps/api/app/models/usuario_contexto.py` (modificar)
- `apps/api/alembic/versions/0003_add_fk_indexes.py` (novo)
- `apps/api/app/services/import_service.py` (modificar)
- `apps/api/app/services/ai_service.py` (modificar)
- `apps/api/app/services/relatorio_service.py` (modificar)
- `apps/api/app/services/abusividade_service.py` (modificar)
