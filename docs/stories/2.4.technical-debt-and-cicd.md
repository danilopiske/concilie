# Story 2.4: Dívida Técnica, CI/CD e Qualidade de Código

## Status

Draft

## Executor Assignment

```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pipeline run", "security scan", "lint audit"]
```

## Story

**As a** desenvolvedor/mantenedor do sistema Concilie,
**I want** ter a dívida técnica mapeada, o pipeline CI/CD hardened e os padrões de qualidade de código estabelecidos,
**so that** o projeto esteja em estado saudável para desenvolvimento contínuo e deploys seguros.

## Acceptance Criteria

1. **AC1 — Pipeline CI/CD unificado**
   - Existe `.github/workflows/ci.yml` que roda em PRs e pushes para `main`
   - Pipeline executa: lint → typecheck → pytest → build
   - Falhas em qualquer step bloqueiam o merge
   - Cache de dependências configurado (npm + pip) para reduzir tempo de CI

2. **AC2 — Variáveis de ambiente e segredos**
   - Todas as variáveis sensíveis estão em `.env.example` (sem valores reais)
   - GitHub Secrets configurados para CI: `DATABASE_URL`, `SECRET_KEY`, `JWT_SECRET`
   - Nenhum arquivo `.env` com dados reais commitado no repositório
   - `apps/api/.env.example` e `apps/web/.env.example` atualizados e documentados

3. **AC3 — Linting e formatação padronizados**
   - `ruff` configurado em `apps/api/pyproject.toml` ou `ruff.toml`
   - `eslint` + `prettier` configurados em `apps/web/`
   - Pre-commit hooks configurados (`.pre-commit-config.yaml`) ou documentado que hooks são opcionais
   - `npm run lint` e `ruff check app/` passam sem erros no estado atual

4. **AC4 — Dívida técnica mapeada**
   - Arquivo `docs/technical-debt.md` criado com inventário das dívidas identificadas
   - Cada item tem: descrição, impacto (H/M/L), esforço estimado, owner sugerido
   - Itens críticos (impacto H) têm issue ou story de follow-up referenciada
   - Dívidas de Story 2.1 (backend), 2.2 (frontend) e 2.3 (testes) consolidadas aqui

5. **AC5 — Testes de arquivos temporários**
   - Arquivos de teste avulsos na raiz (`test_api_connection.py`, `test_db.py`, etc.) são movidos ou removidos
   - `apps/api/` sem arquivos `test_*.py` fora de `apps/api/tests/`
   - Raiz do monorepo sem arquivos de teste soltos

6. **AC6 — Documentação mínima de operação**
   - `README.md` na raiz do monorepo atualizado com: pré-requisitos, como rodar localmente, como rodar testes
   - `apps/api/README.md` com instruções de setup do backend
   - `apps/web/README.md` com instruções de setup do frontend
   - Variáveis de ambiente documentadas nos respectivos `.env.example`

## Tasks / Subtasks

- [ ] **Task 1 — Pipeline CI/CD** (AC: 1)
  - [ ] 1.1 — Auditar workflows existentes em `.github/workflows/`
  - [ ] 1.2 — Criar/consolidar `ci.yml` com stages: lint, typecheck, pytest, build
  - [ ] 1.3 — Configurar cache de npm e pip no CI
  - [ ] 1.4 — Validar que pipeline roda corretamente em PR de teste

- [ ] **Task 2 — Segredos e variáveis de ambiente** (AC: 2)
  - [ ] 2.1 — Auditar arquivos `.env` no repositório (nenhum deve estar commitado)
  - [ ] 2.2 — Criar/atualizar `apps/api/.env.example` com todas as vars necessárias
  - [ ] 2.3 — Criar/atualizar `apps/web/.env.example` com todas as vars necessárias
  - [ ] 2.4 — Documentar no README como configurar as variáveis localmente

- [ ] **Task 3 — Linting e formatação** (AC: 3)
  - [ ] 3.1 — Configurar `ruff` em `apps/api/` (se não configurado)
  - [ ] 3.2 — Verificar/corrigir `eslint` config em `apps/web/`
  - [ ] 3.3 — Rodar `ruff check app/ --fix` e `npm run lint --fix` e registrar resultado
  - [ ] 3.4 — Garantir que lint passa limpo no CI

- [ ] **Task 4 — Mapeamento de dívida técnica** (AC: 4)
  - [ ] 4.1 — Consolidar achados das stories 2.1, 2.2, 2.3 em lista de dívidas
  - [ ] 4.2 — Adicionar dívidas conhecidas do código legado (modules/)
  - [ ] 4.3 — Criar `docs/technical-debt.md` com inventário priorizado
  - [ ] 4.4 — Criar issues/stories de follow-up para itens de impacto H

- [ ] **Task 5 — Limpeza de arquivos de teste soltos** (AC: 5)
  - [ ] 5.1 — Mover/remover arquivos `test_*.py` da raiz do monorepo
  - [ ] 5.2 — Mover/remover arquivos `test_*.py` avulsos em `apps/api/`
  - [ ] 5.3 — Verificar que `pytest apps/api/tests/ -v` ainda passa após limpeza

- [ ] **Task 6 — Documentação mínima** (AC: 6)
  - [ ] 6.1 — Atualizar `README.md` raiz com visão geral + comandos de setup
  - [ ] 6.2 — Criar/atualizar `apps/api/README.md`
  - [ ] 6.3 — Criar/atualizar `apps/web/README.md`

## Dev Notes

### Contexto

O projeto tem 2 workflows CI existentes (da Sprint 3). Esta story consolida e fortalece o CI/CD, limpa débitos acumulados e garante que o projeto está em estado "production-ready" como baseline para o desenvolvimento futuro.

### Arquivos de teste soltos identificados (git status)

```
apps/api/test_api_connection.py
apps/api/test_api_urllib.py
apps/api/test_insert.py
apps/api/test_internal_routes.py
apps/api/test_login_endpoint.py
apps/api/test_login_endpoint_urllib.py
apps/api/test_schema.py
apps/api/test_users.py
test_db.py           # na raiz!
test_download_headers.py
test_health.py
test_system_perf.py
```

Esses arquivos são testes manuais/exploratórios e **não** fazem parte da suite oficial. Devem ser movidos para `apps/api/tests/` se tiverem valor, ou removidos se forem descartáveis.

### Estrutura CI/CD esperada

```yaml
# .github/workflows/ci.yml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r apps/api/requirements.txt
      - run: cd apps/api && ruff check app/
      - run: cd apps/api && pytest tests/ -v

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/web/package-lock.json
      - run: cd apps/web && npm ci
      - run: cd apps/web && npm run lint
      - run: cd apps/web && npm run typecheck
      - run: cd apps/web && npm run build
```

### Dívidas técnicas já identificadas (pré-mapeamento)

| Item | Área | Impacto | Notas |
|------|------|---------|-------|
| `relatorio_service.py` complexidade | Backend | H | Função de cálculo > 100 linhas |
| `reconciliation_core.py` acoplamento | Backend | H | Herança de módulo legado |
| TipTap extension recriada em useEffect | Frontend | M | Sem impacto visual mas ineficiente |
| Módulos legados em `/modules` | Legacy | H | Ainda referenciados pelo backend |
| Testes soltos na raiz | CI/CD | L | Não bloqueiam mas poluem o repo |
| CORS `allow_origins` verificar prod | Segurança | H | Confirmar na story 2.1 |

### Comandos úteis

```bash
# Backend
cd apps/api
ruff check app/ --statistics
pytest tests/ -v --cov=app --cov-report=term-missing

# Frontend
cd apps/web
npm run lint
npm run typecheck
npm run build

# Git — encontrar arquivos .env commitados
git log --all --full-history -- "**/.env"
git ls-files | grep -E "\.env$"
```

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada — dívida técnica e CI/CD | @sm (River) |

## Dev Agent Record

### Agent Model Used
_pendente_

### Debug Log References
N/A

### Completion Notes
_pendente_

### File List
_pendente_

## QA Results

_pendente_
