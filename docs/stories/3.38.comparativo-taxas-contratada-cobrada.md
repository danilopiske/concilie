# Story 3.38: Comparativo de Taxas — Contratada vs Cobrada

## Status
Done

## Executor Assignment
- **Agent:** @dev (Dex)
- **Started:** 2026-03-21
- **Completed:** 2026-03-21

## Story
**As a** analista financeiro,
**I want** ver um comparativo por cliente entre as taxas contratadas e as efetivamente cobradas (média de todos os processamentos),
**So that** posso identificar rapidamente quais combinações bandeira/modalidade apresentam divergências ou situações críticas.

## Contexto Técnico
- `TaxaContratada` model já existe (Story 3.11): campos `bandeira`, `modalidade`, `taxa_contratada` (Float, percentual)
- `VendasCalculos` (tabela `vendas_calculos`): campos `bandeira`, `forma_pagamento`, `tx_venda`, `vl_venda`, `calc_id`
- `calc_id` em VendasCalculos = `str(processamento_id)` — padrão do AbusividadeService
- `forma_pagamento` em VendasCalculos = `modalidade` em TaxaContratada — mesma semântica
- Endpoint existente (Story 3.11): `GET /clientes/{id}/taxas-contratadas/comparacao?processamento_id=X` (por processamento, thresholds ok/atencao/abusivo)
- Story 3.38 cria: `GET /clientes/{id}/taxas-comparativo` — visão geral do cliente (todos os processamentos, thresholds ok/divergente/critico)

## Acceptance Criteria

1. **AC1 — Endpoint backend**
   - `GET /clientes/{cliente_id}/taxas-comparativo` retorna lista de `ComparativoItem`:
     - `bandeira`, `modalidade`: string
     - `taxa_contratada`: float (percentual cadastrado)
     - `taxa_media_cobrada`: float (média de tx_venda de todos os processamentos do cliente)
     - `diferenca`: float (taxa_media_cobrada - taxa_contratada)
     - `status`: 'ok' | 'divergente' | 'critico'
       - 'critico' se diferenca > 0.5 (taxa cobrada excede contratada em mais de 0.5pp)
       - 'divergente' se 0 < diferenca <= 0.5
       - 'ok' se diferenca <= 0
     - `quantidade_transacoes`: int (total de vendas para essa bandeira/modalidade)

2. **AC2 — Schema Pydantic**
   - `ComparativoItem` e `ComparativoResponse` adicionados em `schemas/taxa_contratada.py`

3. **AC3 — Frontend: API client**
   - `taxasComparativoApi.listar(clienteId)` em `apps/web/src/lib/api/taxasComparativo.ts`

4. **AC4 — Frontend: página**
   - `apps/web/src/app/(dashboard)/gestao/clientes/[id]/taxas-comparativo/page.tsx`
   - Resumo no topo: contadores critico/divergente/ok
   - Tabela: bandeira, modalidade, taxa contratada (%), taxa cobrada (%), diferença (%), status badge
   - Badge: verde (ok), amarelo (divergente), vermelho (critico)

5. **AC5 — Navegação**
   - Link "Comparativo de Taxas" adicionado na página de detalhe do cliente (`/gestao/clientes/[id]/page.tsx`)

6. **AC6 — Qualidade**
   - `ruff check app/ --select=E,F,W --ignore=E501` → 0 erros

## Tasks / Subtasks

- [x] **Task 1 — Schema** (AC2)
  - [x] 1.1 — Adicionar `ComparativoItem` e `ComparativoResponse` em `schemas/taxa_contratada.py`

- [x] **Task 2 — Backend** (AC1)
  - [x] 2.1 — Adicionar função `comparativo_geral` em `services/taxa_contratada_service.py`
  - [x] 2.2 — Adicionar endpoint `GET /{cliente_id}/taxas-comparativo` em `endpoints/taxas_contratadas.py`

- [x] **Task 3 — Frontend** (AC3, AC4, AC5)
  - [x] 3.1 — Criar `apps/web/src/lib/api/taxasComparativo.ts`
  - [x] 3.2 — Criar `apps/web/src/app/(dashboard)/gestao/clientes/[id]/taxas-comparativo/page.tsx`
  - [x] 3.3 — Adicionar link na página de detalhe do cliente

- [x] **Task 4 — Qualidade** (AC6)
  - [x] 4.1 — ruff check → 0 erros

## Dev Notes

### Query utilizada (SQL raw via text())
```sql
SELECT
    bandeira,
    forma_pagamento,
    AVG(CAST(tx_venda AS FLOAT)) AS taxa_media,
    COUNT(*) AS quantidade
FROM vendas_calculos vc
JOIN processamentos p ON p.id = CAST(vc.calc_id AS INTEGER)
WHERE p.cliente_id = :cliente_id
  AND vc.tx_venda IS NOT NULL
  AND vc.bandeira IS NOT NULL
  AND vc.forma_pagamento IS NOT NULL
GROUP BY bandeira, forma_pagamento
```

### Thresholds Story 3.38 (distintos de Story 3.11)
- `ok`: diferenca <= 0 (cobrada <= contratada)
- `divergente`: 0 < diferenca <= 0.5pp
- `critico`: diferenca > 0.5pp

## File List
- `apps/api/app/schemas/taxa_contratada.py` — MODIFICADO (ComparativoItem, ComparativoResponse)
- `apps/api/app/services/taxa_contratada_service.py` — MODIFICADO (comparativo_geral)
- `apps/api/app/api/v1/endpoints/taxas_contratadas.py` — MODIFICADO (novo endpoint)
- `apps/web/src/lib/api/taxasComparativo.ts` — CRIADO
- `apps/web/src/app/(dashboard)/gestao/clientes/[id]/taxas-comparativo/page.tsx` — CRIADO
- `apps/web/src/app/(dashboard)/gestao/clientes/[id]/page.tsx` — MODIFICADO (link)

## QA Results
- ruff: 0 erros

## Dev Agent Record
### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Endpoint `GET /clientes/{cliente_id}/taxas-comparativo` criado no router existente (taxas_contratadas.py)
- Thresholds distintos da Story 3.11: ok (<=0), divergente (0-0.5pp), critico (>0.5pp)
- Usa SQL raw para performance (mesma abordagem do _comparar existente)
- Join via `p.cliente_id` em vez de `processamento_id` explícito — visão agregada do cliente
