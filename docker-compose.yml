services:
  api:
    build:
      context: apps/api
    ports:
      - "8000:8000"
    env_file:
      - apps/api/.env
    volumes:
      - ./data:/data  # SQLite database persistido no host
    restart: unless-stopped
